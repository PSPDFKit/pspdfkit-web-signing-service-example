# Deploying signing service example in Kubernetes

## Prerequisites

A prebuilt container image needs to be available.

## Deployment assumptions

It is assumed that resources are to be created in `nutrient-services` namespace.
