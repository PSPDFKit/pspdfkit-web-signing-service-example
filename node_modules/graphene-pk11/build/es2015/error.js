export function isError(error, errorType = Error) {
    return error instanceof errorType;
}
export function prepareError(error) {
    if (isError(error)) {
        return error;
    }
    return new Error(`Unknown error. ${error}`);
}
